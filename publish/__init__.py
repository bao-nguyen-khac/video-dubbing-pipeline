"""
publish/ — Đăng video kết quả lên TikTok/YouTube Shorts qua Zernio
(feature 006-publish-video-tab).

Module thuần Python, không phụ thuộc FastAPI: web/backend/publish_api.py chỉ là
lớp HTTP mỏng gọi lại đây (giống jobs_api.py gọi pipeline.py).
"""
